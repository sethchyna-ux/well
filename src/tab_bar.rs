// src/tab_bar.rs
// Simple TabBar stub for managing terminal tabs.
// This is a minimal implementation to satisfy compilation; later it will be expanded.

#[derive(Debug, Clone)]
pub struct TabBar {
    // Placeholder fields; actual tab management will be added later.
    pub tabs: usize,
}

impl TabBar {
    pub fn new() -> Self {
        Self { tabs: 1 }
    }

    // In future: methods to add, close, switch tabs, etc.
}
